# 패밀러리(Familery) 프로젝트

## 한 줄 소개

패밀러리는 `Family + Gallery`를 합친 이름으로, 가족들이 여행과 모임에서 촬영한 사진을 쉽고 안전하게 모아 보는 웹 기반 가족 사진첩이다.

## 프로젝트 목적

이 프로젝트에는 두 가지 목적이 있다.

1. React와 Next.js를 실제 프로젝트를 통해 학습한다.
2. 고령자를 포함한 가족 누구나 설명 없이 사진을 올리고 볼 수 있는 서비스를 만든다.

기술 기반은 **Next.js App Router + React + TypeScript**로 한다.

## 핵심 사용자 경험

- 가족 사진을 앨범 단위로 모은다.
- 여러 사용자가 올린 사진을 촬영시간 순서로 감상한다.
- 이전·다음 버튼, 모바일 스와이프, 자동 슬라이드를 제공한다.
- 크게 보기 모드에서는 글자뿐 아니라 버튼, 터치 영역, 간격도 함께 키우고 부가 정보를 줄인다.
- 스와이프나 아이콘을 유일한 조작법으로 사용하지 않는다.
- `이전 사진`, `다음 사진`, `자동으로 보기`처럼 뜻이 적힌 큰 버튼을 제공한다.
- 목표는 “설명하지 않아도 사진을 올리고, 설명하지 않아도 사진을 볼 수 있게 한다”이다.

## 화면 구성

- 로그인 화면
- 앨범 목록 화면
- 앨범 상세 및 슬라이드 갤러리 화면
- 개인 설정 화면
- Owner 전용 앨범 관리 화면
- 삭제 확인, 사진 선택, 초대 공유, 비밀번호 입력용 팝업 또는 다이얼로그

개인 설정과 Owner 전용 앨범 관리는 구분한다.

### 개인 설정

- 크게 보기 기본값
- 자동재생 기본값
- 로그아웃

### Owner 전용 앨범 관리

- 앨범 이름, 날짜, 위치
- 초대 링크 생성 및 공유
- 멤버와 권한 관리
- 앨범별 비밀번호 관리
- 앨범 삭제

## 역할과 권한

### Owner

- 앨범을 만든 사용자다.
- 앨범 설정, 초대, 멤버 권한, 비밀번호를 관리한다.
- 사진을 업로드할 수 있다.
- 누가 올린 사진이든 삭제할 수 있다.

### Editor

- 유효한 초대 링크 등을 통해 앨범 편집 권한을 얻은 사용자다.
- 사진을 업로드할 수 있다.
- 자신이 업로드한 사진만 삭제할 수 있다.

### Viewer

- 현재 구현 범위에는 포함하지 않는다.
- 추후 열람 전용 역할이 필요할 때 확장한다.

프론트에서 버튼을 숨기는 것만으로 권한 처리를 끝내지 않는다. 실제 서비스에서는 서버가 로그인, 앨범 멤버십, 역할, 사진 업로더, 사진의 앨범 소속을 다시 검증해야 한다.

## 초대와 재접속

- Owner가 초대 URL을 생성한다.
- Web Share API 등 운영체제 공유 인터페이스를 사용해 카카오톡 등으로 전달한다.
- 유효한 링크로 최초 진입한 사용자는 해당 앨범의 Editor 권한을 얻는다.
- 초대 토큰은 1회성 또는 만료 방식을 검토한다.
- 링크를 잃어버린 경우를 위해 앨범마다 서로 다른 비밀번호를 설정할 수 있다.
- 비밀번호 원문은 저장하지 않고 해시만 저장한다.
- 비밀번호 인증만으로 새 Editor를 무조건 만들기보다 기존 멤버의 권한을 복원하는 방향을 우선 검토한다.

## 사진과 정렬

- 여러 사용자가 올려도 업로드 시간이 아니라 EXIF 촬영시간인 `takenAt` 기준으로 정렬한다.
- EXIF 촬영시간이 없는 사진에는 fallback 정책이 필요하다.
- 초기에는 업로드 시간을 fallback으로 사용하거나 사용자가 촬영시간을 수정하는 방식을 검토한다.
- 앨범 생성 시 여행·모임의 날짜와 위치 범위를 설정한다.
- 향후 사용자가 사진을 선택한 뒤 EXIF 촬영시간과 GPS를 분석해 앨범에 맞는 후보를 추천한다.
- 웹 브라우저는 휴대폰 사진첩을 임의로 탐색할 수 없으므로, 반드시 사용자가 먼저 사진을 선택해야 한다.

## 갤러리 기능

- 촬영시간 순 정렬
- 현재 사진 한 장 렌더링
- 이전·다음 순환 이동
- 모바일 스와이프
- 기본 5초 간격 자동 슬라이드
- 자동재생 ON/OFF
- 크게 보기 모드
- 권한에 따른 삭제 버튼 표시

## 예상 데이터 모델

```ts
type User = {
  id: string;
  name: string;
};

type Album = {
  id: string;
  ownerId: string;
  title: string;
  dateRange: unknown;
  location: unknown;
  passwordHash: string | null;
};

type AlbumMember = {
  userId: string;
  albumId: string;
  role: "owner" | "editor" | "viewer";
};

type Photo = {
  id: number;
  albumId: string;
  uploaderId: string;
  takenAt: string;
  uploadedAt: string;
  imageUrl: string;
};
```

이 타입은 방향을 설명하기 위한 초안이며 구현 단계에서 구체화한다.

## 구현 로드맵

### 1단계: React 갤러리 실습

- Next.js + TypeScript 프로젝트 생성
- 가짜 앨범과 사진 데이터 8~10장
- `takenAt` 기준 정렬
- 현재 사진 렌더링
- 이전·다음 순환 이동
- 5초 자동 슬라이드와 자동재생 ON/OFF
- `useEffect`, `setInterval`, cleanup 연습
- 크게 보기 모드
- 가짜 `currentUser`와 Owner/Editor 역할
- 권한에 따른 삭제 버튼 조건부 렌더링
- 더미 데이터에서 사진 삭제

이 단계에서는 로그인, 실제 업로드, EXIF, GPS, 서버 권한, 초대, 비밀번호, Storage를 구현하지 않는다.

### 2단계: 사진 선택과 메타데이터

- 사용자가 사진 선택
- EXIF 촬영시간과 GPS 분석
- 앨범 날짜와 위치에 따른 후보 필터
- 촬영시간 fallback 처리

### 3단계: 인증과 서버 권한

- 실제 로그인과 세션
- `AlbumMember` 기반 권한
- 서버에서 업로드·삭제 권한 검증

### 4단계: 초대와 비밀번호

- 초대 URL과 토큰
- Web Share API
- 앨범별 비밀번호 해시와 검증
- 기존 멤버 권한 복원

### 5단계: 실제 파일 저장

- Storage 연결
- 실제 사진 업로드와 삭제
- 로딩, 성공, 실패 상태 처리

## 현재 위치

- 프로젝트 루트와 `AGENTS.md`, `PROJECT.md`를 준비했다.
- 아직 Next.js 프로젝트를 생성하지 않았다.
- 다음 작업은 Node.js와 npm 버전을 확인한 후 현재 루트에 Next.js 프로젝트를 생성하는 것이다.
